<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyTriviaApp.Views.BestScoresPage"
             xmlns:vm="clr-namespace:MyTriviaApp.ViewModels"
 x:DataType="vm:BestScoresPageViewModel"
             Title="BestScoresPage">

    <ScrollView>

        <StackLayout>
            <RefreshView Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsRefreshing}">
                <CollectionView x:Name="colview" ItemsSource="{Binding Players}" EmptyView="לא נמצאו משתמשים.">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout ItemSpacing="5" Orientation="Vertical"/>
                    </CollectionView.ItemsLayout>
                </CollectionView>
            </RefreshView>
        </StackLayout>

    </ScrollView>

</ContentPage>